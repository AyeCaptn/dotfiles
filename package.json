{
    "name": "dotfiles",
    "version": "0.0.0",
    "private": true,
    "scripts": {
        "lint": "eslint . --cache --fix",
        "test": "pnpm run lint",
        "posttest": "pnpm run format",
        "format": "prettier --loglevel warn --write \"**/*.{js,md}\""
    },
    "engines": {
        "node": ">=8.9"
    },
    "dependencies": {
        "diff-so-fancy": "^1.3.0",
        "eslint": "^7.9.0",
        "prettier": "^2.1.2",
        "trash-cli": "^3.1.0",
        "serverless": "^3.3.0"
    }
}